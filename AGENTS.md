# DriveSense-VLM — agent instructions

Full project instructions — architecture decisions, key paths, commands, output
schema, and coding rules — live in [CLAUDE.md](CLAUDE.md). It is the single
source of truth for any agent working in this repo. Read it first.

This file exists only so tools that look for `AGENTS.md` by convention find the
same guidance. It deliberately does not copy CLAUDE.md: maintaining two full
copies is how they drift out of sync.
